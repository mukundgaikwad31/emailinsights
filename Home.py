import streamlit as st
def app():
#     with open("style.css") as f:
#         css = f.read()

# # Inject the CSS
# st.markdown(f"<style>{css}</style>", unsafe_allow_html=True)
    st.subheader("What is Email Insights?")
    st.write(
    """
    Email Insights leverages cutting-edge AI technologies to automatically categorize and process incoming emails. 
    By analyzing email content and metadata, our system classifies emails into different categories like 
    support, inquiries, promotions, and more, improving workflow efficiency.
    """
    )

    st.subheader("Key Features")
    st.markdown("""
    - **AI-Driven Categorization:** Automatically classifies emails into predefined categories.
    - **Text Analytics:** Analyzes
        """)